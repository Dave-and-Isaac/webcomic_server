services:
  stripstash:
    image: ghcr.io/dave-and-isaac/webcomic-reader:latest
    container_name: stripstash
    ports:
      - "8000:8000"
    environment:
      - COMICS_DIR=/comics
    volumes:
      - ./comics:/comics:ro
      - ./config:/app/config
      - ./data:/app/data
