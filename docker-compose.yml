services:
  webcomic_reader:
    image: ghcr.io/dave-and-isaac/webcomic-reader:latest
    ports:
      - "8000:8000"
    environment:
      - COMICS_DIR=/comics
    volumes:
      - ./comics:/comics:ro
      - ./config:/app/config
      - ./data:/app/data
